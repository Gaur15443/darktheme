import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    divider: {
        justifyContent: 'center',
        alignItems: 'center',
        paddingVertical: 20
    },
    cardTitle: {
        color: "#27C394",
        fontSize: 14,
        paddingBottom: 8
    },
    cardDescription: {
        fontSize: 12,
        paddingHorizontal: 11
    },
    mainTitle: { textAlign: 'center', paddingTop: 24 },
    cardContainer: { backgroundColor: '#FFFFFF1A', borderRadius: 20, padding: 14 },
    cardHeading: { textAlign: 'center', fontSize: 16 },
})

export default styles;